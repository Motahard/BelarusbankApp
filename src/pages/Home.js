import React, { Fragment } from 'react';
import CityForm from '../components/CityForm';

const Home = () => {
  return (
    <Fragment>
      <CityForm />
    </Fragment>
  );
};

export default Home;
